string carBrand = 'Honda';
system.debug(carBrand);

string model = 'Accord';
system.debug(model);

string color = 'Red';
system.debug(color);

string myCar = carBrand + ' ' + model + ' ' + color;
system.debug(myCar);